export default () => {
  return (next) => (action) => {
    // const { promise, types, ...rest } = action
    
    // if ( ! promise) {
    //   return next(action)
    // }
    
    // const [ REQUEST, SUCCESS, FAILURE ] = types 
    // next({ ...rest, type: REQUEST })
  }
}